server:
  port: 8080

spring:
  application:
    name: user-profile-service

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://pg-payvault-pg-payvault.k.aivencloud.com:21627/defaultdb?sslmode=require
    username: avnadmin
    password: AVNS_P9VRhnFKq97GIU5skEo

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true
